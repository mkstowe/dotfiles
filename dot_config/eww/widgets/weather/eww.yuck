(defpoll weather :interval "5m" "~/.config/eww/scripts/weather")

(defwidget weather []
    (box
        :class "weather"
        :orientation "h"
        :width 50
        (label :text "${weather}")
    )
)