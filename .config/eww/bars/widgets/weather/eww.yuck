(defpoll weather :interval "5m" "./scripts/weather")

(defwidget weather []
    (box
        :class "widget weather"
        :orientation "h"
        :width 50
        (label :text "${weather}")
    )
)
